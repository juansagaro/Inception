[mysqld]
# Daemon user
user            = mysql

# Default port
port            = 3306

# Data directory (matches Docker volume)
datadir         = /var/lib/mysql

# Socket path (matches dir created in Dockerfile)
socket          = /run/mysqld/mysqld.sock

# Listen on all interfaces (required for WordPress connection)
bind-address    = 0.0.0.0
